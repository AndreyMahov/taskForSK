spring:
  application:
    name: test
  datasource:
    url: jdbc:h2:mem:credit_service
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
